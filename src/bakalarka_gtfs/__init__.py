"""
bakalarka_gtfs — GTFS editor powered by LLM agent + MCP server.

Subpackages:
    agent   — GTFSAgent class, system prompt, pricing
    api     — FastAPI OpenAI-compatible endpoint
    core    — Shared configuration
    mcp     — MCP server, database, patching, visualization
"""

__version__ = "0.3.0"
